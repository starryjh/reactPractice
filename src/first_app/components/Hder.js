import React from 'react'
import Nav from './Nav'

const Hder = ()=>{
    return (

    <div>
        <header>
            <h1>{props.logo}</h1>
        </header>
        <Nav />
    </div>

        )
 
}

export default Hder